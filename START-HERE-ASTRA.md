# Paste this instruction into GPT-6 Astra

Use the attached Porli concept pack to create a GitHub repository called `porli` under the account or organisation I identify. Keep it private unless I explicitly request otherwise. If the owner cannot be resolved, prepare the repository locally and ask only for that missing destination. Do not claim to have pushed it without a successful remote push.

First establish this documentation as the repository source of truth. Preserve its folder structure, add an initial documentation commit and report the repository link when available. This request establishes the repository; do not interpret it as approval to launch a public service or purchase domains.

Read README.md, AGENTS.md and docs/02-scope-and-decisions.md before implementation. Distinguish confirmed user requirements from proposed defaults. Preserve British English. Porli is the selected working name, pronounced POR-lee, with tagline “Find your next place.” Name, domain and trade mark availability remain unchecked.

Prepare an implementation plan and turn the phased backlog into actionable work items. If asked to continue with development, build one complete vertical journey first: public discovery, property detail, account, enquiry, staff reply and inspection request. Keep the fixed listing template and single-organisation operating model. Do not introduce public agent onboarding, payments, AI chat or screening without a scope decision.

Use a considered editorial design: warm ivory, ink, olive, expressive serif headlines and readable interface typography. Follow the included visual rules and image prompts. Use Higgsfield for fictional concept imagery if available. Do not pretend an image was generated or an integration connected when it was not. Do not represent generated homes as real listings.

Inspect the existing repository and environment before choosing libraries or infrastructure. Propose and document a compatible stack; validate current provider documentation before integrating it. Keep deployment compatible with the agreed host. Do not hard-code a host assumption into the product.

Use fictional fixture data only for the prototype, clearly marked. A production implementation must persist records, authenticate users, enforce server-side access and protect private messages and notes. A simulated prototype must say exactly which features are simulated. Keep secrets out of Git and provide environment variable names and setup documentation when services are selected.

When complete, report: repository URL and branch, what exists, what is working, what is simulated, checks performed, unresolved launch decisions and the next development milestone. Keep specifications aligned with implementation.
