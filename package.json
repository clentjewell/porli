{
  "name": "porli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check lib/porli.mjs && node --check worker/index.mjs && node --check public/app.js && node --check public/experience.js",
    "build": "npm run check",
    "cf:dev": "npx wrangler dev --var PORLI_DEMO:1",
    "cf:deploy": "npx wrangler deploy"
  }
}
